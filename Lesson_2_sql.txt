Виктор, приветствую!

1. Установите СУБД MySQL. Создайте в домашней директории файл .my.cnf, задав в нем логин и пароль, который указывался при установке

Создал в домашней дирректроии файл .my.cnf

ubuntu@ip-172-31-37-69:~$ sudo touch .my.cnf

В nano прописал логин/пароль
ubuntu@ip-172-31-37-69:~$ sudo nano .my.cnf
Сразу прописал Client, чтоб распространялось на все не только на SQL. Возможно ли ничего не указывать, а прописать лишь логин и пароль?
[client]
user=root
password=******

Оставил доступ к файлу только владельцу
sudo chmod go-r .my.cnf
Сменил группу. хотя доступ из групп запретил. Это на всякий случай, вдруг кому то нужно будет дать доступ, например группе)). Больше для вспомнить материал.
sudo chown ubuntu ./.my.cnf

2. Создайте базу данных example, разместите в ней таблицу users, состоящую из двух столбцов, числового id и строкового name.

Создал БД.
create database example;

Перешел в базу.
use example;

Создал таблицу.
create table users (id int auto_increment not null primary key, name varchar(50) not null);

Добавил одну строку в таблицу, чтоб там что то было.
insert into users (name) values("Sergey");

3. Создайте дамп базы данных example из предыдущего задания, разверните содержимое дампа в новую базу данных sample.

тут я немного поплясал и погуглить пришлось, плюс вспомнить перенаправление потоков вывода.
Сделал дамп сразу заархивировал его, опять же чтоб вспомнить )
mysqldump example | gzip > exampleBU.sql.gz

Создал новую базу sample.  Как я понимаю, создать базу и сразу туда  залить бэкап не получится?
create database sample;

загрузил в нее архив
gunzip < exampleBU.sql.gz | mysql sample

Моя таблица users с одной записью оказалась в новой БД sample.

4. (по желанию) Ознакомьтесь более подробно с документацией утилиты mysqldump. Создайте дамп единственной таблицы help_keyword базы данных mysql. 
Причем добейтесь того, чтобы дамп содержал только первые 100 строк таблицы.

не без поисков в инете получил это:(по id отсортировал еще)
mysql -e "select * from help_keyword order by help_keyword_id limit 100" mysql > mydumpfile2.txt

Другие способы почему то не работают например этот:
mysqldump -e mysql help_keyword --where "limit 5" > mydumpfile2.txt


Урок второй
Можно ли куда-то кроме домашней дирректории "засунуть" файл .my.cnf? и нужно ли ограничивать доступ к нему и какие нужны права, для безопасности?

Что нужно сделать, чтоб восстановить содержимое дампа таблицы? у меня никак не получилось.
